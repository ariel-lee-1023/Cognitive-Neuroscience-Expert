# Validation record

Validation is performed on the staged copy before integration, then checked at the final checkout. See validation.json for the actual generic validator output and existing-architecture-check.json for the explicit existing-layout assessment. The initial failed checks are retained with an initial- prefix: a table-shortening expression removed 18 links; restoring complete links and shortening triggers repaired that issue. The initial audit index was also incomplete. No runtime was published in that state.

The repository-specific link/provenance checker, separate core/reference instruction scans, source-count and token-cap checks, symlink resolution, and prior-file hashes are recorded alongside this file. LOW external-link scanner findings are research citations, not executable instructions. Source text was treated as data throughout. Generic layout objections to the established demos/maintenance/scripts/tests directories are accepted under the user's existing-architecture instruction; these are not deleted to make the generic check green. Existing reference-budget warnings are preserved and are outside this fold-in.

No computational code changed; numerical demonstrations were checked for byte preservation rather than rerun. Editorial scenarios are documented in evaluation.md; fresh-context behavioral acceptance and earlier behavioral regressions are unrun. Passing structural checks does not establish improved model reasoning or empirical intervention efficacy.

Final staging result: 21 source references; master body 4,297 estimated tokens (4,500 cap); new references 3,935 / 4,559 / 4,458 tokens, all below budgets and caps. Zero new structural/routing errors; zero local link/provenance errors; 34 existing correction records preserved; zero HIGH/MEDIUM scanner findings and 24 LOW external-link findings reviewed as citations. All 18 earlier source references and other staged baseline files apart from SKILL.md/README.md are byte-identical.
