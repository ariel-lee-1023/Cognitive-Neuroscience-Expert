# Validation record

The staged project was checked before integration into the existing checkout. `validation.json` is the actual generic `validate_library.py --layout published-repo` output; `baseline-validation.json` records the same tool against the unmodified destination. Both report directory-layout errors for demos/, maintenance/, scripts/, and tests/. The original checkout also has an ignored outputs/ directory; it was not copied into staging and is preserved at the final location. These directories belong to the user’s established architecture. They are preserved under the explicit existing-destination exception, not silently deleted or reported as a generic validator pass. `existing-architecture-check.json` requires every staging layout error to be pre-existing and no new structural errors.

The core body is **4,438 estimated tokens**, below 4,500. All **23 source references** are directly reachable; the new references are **7,094** (Health) and **7,737** (Neural Design) estimated tokens. All file caps pass. The generic warnings about two old budget overruns, an old absent section count, requirements-demo.txt, and the optional demonstration guide remain unchanged. Neural Design is below its provisional planning budget by selection, not truncation or padding.

The repository’s reference/provenance checker reports zero errors and preserves all 34 earlier correction records. Its first run found the not-yet-written new ledger README; that initial result is retained, and the final run after ledger creation passes. Its 438 categorical-language flags include 35 in the new references. New flags were inspected as conditional rules, source-attributed positions, or scope qualifications; they are not treated as semantic proof. The separate core and references instruction scans report zero HIGH/MEDIUM findings. All 27 LOW findings are citation URLs, including the three new primary checks.

The skill-creator quick validator first failed because the system Python lacked PyYAML. It passed using the existing Anaconda Python; no package was installed. Four mathematical sanity checks were executed for the reconstructed spike count, entropy, flat-spectrum rate, and CDF mapping. These do not validate biological optimality or interventions.

Prior-file hashes require every previously tracked file except SKILL.md and README.md to remain byte-identical. This preserves all 21 earlier references, demonstrations, tests, maintenance, and historical evaluation records. No executable code changed; numerical demonstration tests were therefore not rerun. Symlink target is still `../..`, resolving to the canonical root. Relative runtime and new ledger links were checked. Full source files and extraction scratch remain outside the deliverable.

The reading report is an expenditure/coverage aid with explicitly recorded truncation limits. The health book exceeded the four-times planning alert while new sections and exceptions were investigated; additional reading was bounded to unresolved content. This is neither exhaustive source validation nor a measure of provider usage.

Fresh-context behavioral acceptance and previous per-task behavioral regressions remain **unrun**, as recorded in acceptance-status.json and evaluation.md. No passing response records or model-performance gains are claimed. Final-location checks verify integration and runtime hashes before commit; remote verification occurs after push and is reported to the user.
